Properties
name
xinghe-backtest-pipeline
description
星核天启量化平台公式参数自动调优流水线（v2 主流程：LHS粗筛→结构探测分杠杆→TPE软约束贝叶斯→批量月度"不能连负"筛查→精简XLSX报告；v1 旧流程RF-SMBO保留为备选）。优化目标恒为【最大化本金收益率 pr】，按用户给定 sig 可行区间取可行解。当用户要求对星核公式做批量回测调参、贝叶斯/随机森林优化参数、按本金收益率筛选配置、按月度"不能连负"筛选、或生成回测XLSX报告时使用。含完整脚本、MCP接口用法、经验教训、报告格式规范。
agent_created
true
星核天启量化平台 · 公式参数自动调优流水线
优化目标（用户定稿，2026-08-31）
唯一目标：最大化本金收益率 pr。 信号数只作为可行区间门槛（如 sig∈[150,500]）， 不是优化目标。不要问用户账户规模、不要优化年化、不要搞多目标——就找最大的 pr。

pr  = totalProfitAmount ÷ minimumInitialCapital     ← 主目标，最大化
sig = stockCount                                     ← 可行区间门槛
ppt = totalProfitAmount ÷ stockCount                 ← 诊断指标（元/信号）
三个指标的正确用法（务必理解，否则会选错配置）
指标	分母	性质	用途
pr	MIC（随 sig 增大）	被信号数稀释	唯一优化目标
ann（平台返回）	固定 ~102.1 万	失真，勿用	仅作参考列
ppt	无	单笔盈利能力	诊断：pr 上不去时看是数量还是质量问题
ann 陷阱（重要）：实测平台 annualizedReturnRate 的分母恒为 102.1 万，不随策略变， 即 ann = 总盈利 × 常数。所以 ann=142.78% 的配置真实回报率只有 36%。 绝不能用 ann 当优化目标——它等价于优化总盈利，会让优化器往高 sig 跑，且数字严重虚高。

pr 稀释机制：pr ≈ (ppt/k) × sig^0.1，即 pr 几乎不随 sig 增长（实测 corr(pr,sig) = −0.086）。 所以优化 pr 天然会选"少而精"的配置——这是正确行为，符合用户目标。

何时使用
用户要求对星核公式做批量回测调参、找最优参数
用户提到"本金收益率""每月收益""不能连负""贝叶斯""TPE""网格搜索""粗筛精筛"
用户给出因子范围+步长表，要求自主搜索
用户要求生成含参数/月度明细/通过名单的 XLSX 报告
开始前需要用户提供的（只问这些，别问账户规模）
TOKEN（过期症状=接口返回空而非报错，重抓后必须重启 server）
公式的平台准确名称（id 用 get_user_formular_by_name 自动查，别手写）
回测日期区间（改 pipe_common.py 的 START_DATE/END_DATE，全程固定）
每个因子的范围+步长表（写成 formula_factors.json 的 user_bounds + step_grid）
sig 可行区间（如 [150,500]）
环境准备
pip install fastmcp optuna openpyxl        # v2 不需要 scikit-learn
MCP server 需在 127.0.0.1:8000 监听（HTTP）。机器重启后不会自启，每次开工先查端口。
零信号排查走独立技能 xinghe-zero-signal-triage（查Step0公式是否存在→端口→token→全开配置判健康→区分死区vs串扰）。
v2 主流程（2026-08-31 起爆5 验证：pr 33.44% → 55.53%，584 组真回测）
全部脚本在 scripts/v2/，用自己的 pipe_common.py（与 v1 旧脚本不混用）。

阶段1 LHS 粗筛 200组 ──► 阶段2 结构探测（5 个小实验，~150组）──► 阶段3 TPE 贝叶斯 208组
      │                        │                                        │
      └─ 找活点                └─ 分出 质量杠杆/数量杠杆/免费维度        └─ 软约束目标+热启动种子
                                                                          │
                                                                          ▼
                                  阶段4 批量月度筛查（不能连负）──► 阶段5 精简 XLSX
阶段1 · LHS 粗筛（pipe_s1_coarse.py，~200 组，~2h）
从零开始、不依赖任何先验。

必须做列内置换：LHS 每列独立 rng.permutation，否则所有列共用同一分层顺序， 样本沿对角线走（开头全 lo、结尾全 hi），整批作废。这个 bug 曾让 200 组白跑。
先跑 --verify（8 组 + 3 次活锚点复测）确认串扰被压住。
锚点必须是已知 sig>0 的活配置（死锚点 n/a 对 n/a，校验毫无意义）。
记录每组 pr/sig/ann/wr + 因子值，落盘 CSV。
阶段2 · 结构探测（本流程最大价值所在，~150 组）
粗筛只是找活点；真正出结果靠这 5 个实验把因子分类：

实验	脚本	作用
OFAT 邻域	pipe_s2_fine.py	每因子 lo±1步/hi±1步/两端放开。按 ppt 变化把因子分成三类
2D 交互网格	pipe_s2_grid.py	两个关键因子交叉，看是否存在正向交互
放松阶梯	pipe_s2_ladder.py	从质量核逐级放宽，测每个杠杆的「边际信号 / 边际 ppt 代价」
连续杠杆细扫	pipe_s2_sweep62*.py	对数量杠杆做细粒度扫描（见下方步长陷阱）
质量回补	pipe_s2_quality.py	把为凑信号而放宽的维度收回来，改用连续杠杆补信号
三类因子（分类后才能高效搜索）
质量杠杆（收窄 → ppt 翻倍，但 sig 下降）：如 10 分钟涨跌幅、636 趋势线、407 主力净流入
数量杠杆（放宽 → sig 翻倍，但 ppt 崩塌）：如 62 大单、9 分时量比
免费维度（改了结果完全不变）：直接锁死不搜，省预算。识别方法：OFAT 里 sig/pr/ppt 三项全相同
关键策略：用连续数量杠杆凑信号，而不是放松质量维度
早期为凑信号去放宽质量维度是陷阱（代价 −66% ppt）。正确做法： 质量维度收紧到最优，再用连续数量杠杆（如 62 的 lo）把 sig 拉回可行区。 这一招在起爆5 上让可行区 pr 从 21% 提到 55.53%。

⚠️ 步长陷阱（必查）
用户给的步长可能过粗（如 62 步长 1000，导致 sig 只能在 32 与 238 间跳变，中间够不到 150）。 实测平台接受任意整数，不强制步长。 遇到"够不到目标 sig"时，无视步长做细粒度扫描验证。

阶段3 · TPE 贝叶斯（pipe_s4_tpe.py，~200 组，~75min）
软约束目标函数（关键，硬门槛会让 TPE 在边界乱撞）：
sig == 0          → score = -10
sig ∈ [lo,hi]     → score = pr + 0.05        # 可行区小奖励
sig < lo          → score = pr - 0.05 - 0.60*(lo-sig)/lo
sig > hi          → score = pr - 0.05 - 0.40*(sig-hi)/hi
空间压缩，绝不全局撒点（用户定稿思路，2026-09-01）：阶段2 的因子分类直接决定阶段3 搜索域， 8 因子压到 4~7 维有效搜索，预算只花在有效维度上。分类 → 空间处理映射：
免费维度（改了结果完全不变）→ 整维固定，不搜，省预算
无差异维度（某端改了没区别，如某因子 hi>12 无差异）→ 只搜一端，另一端固定
质量主杠杆 → 只在验证过的窄带内精搜（lo 窄带 + 小跨度）
连续数量杠杆 → 给足分辨率（步长取细），用来把 sig 顶回可行区
枚举型小集合 → 直接枚举候选（如 hi∈{1,2}）
示例（思路参考，因子号不是通用结论，换公式必须按本公式阶段2 实测重新判定）： 419 固定[0,8]、634 lo=0 只搜 hi∈[10,18]、10 lo∈[3,6] 跨度∈[1,3]、 62 lo∈[0,1000]步50 hi∈[2000,4000]、214 hi∈{1,2}
热启动种子：把阶段2 已知最优配置 enqueue_trial 进去（这些是自主搜出来的，非用户先验）。 种子的第一作用是串扰哨兵：种子跑出的 sig/pr 必须与阶段2 逐一精确复现； 一旦不一致 = 串扰或 token 过期，立刻停跑排查，别浪费 200 组预算。
软约束效果自检（必做）：高 pr 但带外的点必须被正确压低，模型才算学到了"不可行"。 起爆6 实测：sig=123/pr=42.45% → score 仅 0.27，低于带内 sig=159/pr=24.81% 的 0.298， 说明软约束惩罚生效，不是被硬门槛弹开。
TPESampler(multivariate=True) 可捕捉因子间交互。
每 40 组插一次活锚点，每次结果立即追加落盘。
阶段4 · 批量月度筛查（pipe_s5_batch_monthly.py，~45min/13组）
对候选 top N 逐个跑 8 个月切片 + 全窗口复核。

判据 不能连负：连续 2 个月净利润为负 → 不通过。另记零信号月、最长连负、负月名单。
校验：月度 sig 之和应等于全窗口 sig（相等则无跨月重复计数）。
这一步能筛出单看 pr 选错的：起爆5 里 pr 排第二的 t178 有 7 月亏损，而 t201/t204 零负月。
阶段5 · 精简 XLSX（make_report_simple.py）
4 个 sheet，不要做成十几个：

1_结论与推荐 — 冠军配置完整参数 + 关键结论 + 指标陷阱提醒
2_配置清单_含月度判定 — 参数范围 + sig/pr/胜率/总盈利/MIC + 月度判定（通过=绿、有负月=黄、不通过=红）
3_月度收益明细 — 每配置×每月的净利润/收益率，负月标红
4_全部回测记录 — 所有 sig>0 的回测，按 pr 排序，可直接抄参数
格式：百分比预先 ×100 并套 '"%"' 数字格式；参数按因子顺序分列。详见 references/xlsx_format.md。

通用工程纪律（血泪，违反会毁掉整批数据）
防串扰：写完配置 sleep 15s 再回测。平台配置写入有延迟，立刻回测会读到上一组配置。 每次真回测成本 ~22s（含 15s 等待）。全程活锚点复测漂移应 0.00pp。
回测假阴性：配置有效时也有 30~40% 概率首次返回空 statistic。 stockCount 为空时必须 sleep 12s 重试一次，仍空才判 0 信号。
锚点只做串扰哨兵：锚点 pr 变化不代表策略结果变化。判断串扰看 sig 是否精确复现。 （实测锚点 pr 曾从 1.92% 变 2.05%，原因：锚点 MIC 3.06亿 > 平台资金 1亿，是唯一受资金上限 影响的配置；所有 MIC<1亿 的策略 pr 全部复现无偏差。）
模块级 import 放文件顶部：import json 写在 if __name__ 里，被别的脚本 import 时会 NameError。
Optuna 的 objective 是同步回调：协程 rich_trial 必须用独立未运行的 loop （LOOP = asyncio.new_event_loop(); LOOP.run_until_complete(...)），不能复用正在跑的 loop。
默认参数早绑定：def f(token=TOKEN) 在导入时求值。token 改运行时读取后必须写成 def f(token=None): token = token or TOKEN。
增量落盘：长任务每行结果立即 flush()，进程被杀不丢进度。
v1 旧流程（RF-SMBO，备选）
run_pipeline.py 一键跑 Stage0 EDA → S1 TPE300 → S2 SMBO100 → S3 收割150 → S4 月度 → 报告。 脚本在 scripts/（非 v2）。新任务默认走 v2，v1 仅在需要 EDA 边界自动生成（用户提供最宽回测 CSV）时使用。

v1 的 RF 替身模型有外推幻觉风险（曾 17/18 假阳性），v2 改用全真回测打分更稳。

关键文件
路径	说明
scripts/v2/pipe_common.py	v2 共享层：回测、重试、落盘。改日期/端口只改这里
scripts/v2/pipe_s1_coarse.py	LHS 粗筛 + --verify 防串扰验证
scripts/v2/pipe_s2_*.py	结构探测 5 个实验（fine/grid/ladder/sweep62/sweep62fine/quality/combo/push*）
scripts/v2/pipe_s4_tpe.py	TPE 软约束贝叶斯 + 热启动种子
scripts/v2/pipe_s5_batch_monthly.py	批量月度筛查（不能连负）
scripts/v2/make_report_simple.py	4-sheet 精简报告
scripts/v2/pipe_diag_zero.py	零信号诊断（也可走独立技能）
收尾
报告生成后读回验证表头与格式。
汇报：冠军配置的完整参数、pr、月度判定、可行区备选数、报告路径。
提醒未做样本外验证（单窗口结果）。
参考文档
references/mcp_interfaces.md — 全部 13 个 MCP 接口签名/字段/限制
references/lessons_learned.md — 血泪教训（v1 25 条 + v2 新增）
references/xlsx_format.md — 报告格式规范
references/workflow_20260831.md — 起爆5 完整实战记录（584 组，pr 33.44%→55.53%）
